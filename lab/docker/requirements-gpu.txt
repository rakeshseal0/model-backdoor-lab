# AWS inference container. torch is installed separately in the Dockerfile
# from the cu121 index — do not add it here or pip will pull a wheel that
# does not match the base image's CUDA.

transformers==4.44.2
peft==0.12.0
trl==0.9.6
datasets==2.21.0
accelerate==0.33.0
safetensors>=0.4.3

numpy>=1.24,<2.0
scikit-learn>=1.3
joblib>=1.3

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
